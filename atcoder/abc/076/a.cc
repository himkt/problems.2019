//#define _GRIBCXX_DEBUG
#include <bits/stdc++.h>
# define rep(i, n) for (int i = 0; i < (int)(n); i++)
using namespace std;

int main() {
  double a, b;
  cin >> a >> b;
  cout << 2 * (b - (a / 2)) << endl;
  return 0;
}

