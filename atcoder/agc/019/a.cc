//#define _GRIBCXX_DEBUG
#include <bits/stdc++.h>
# define rep(i, n) for (int i = 0; i < (int)(n); i++)
using namespace std;

int q, h, s, d;
double n;
vector<long long> price(4);
vector<double> l = {0.25, 0.5, 1, 2};

vector<vector<vector<int>>> dp;

int main() {
  cin >> q >> h >> s >> d >> n;

  
  return 0;
}

